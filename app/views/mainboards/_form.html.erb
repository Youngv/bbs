<%= form_for(@mainboard) do |f| %>
  <% if @mainboard.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mainboard.errors.count, "error") %> prohibited this mainboard from being saved:</h2>

      <ul>
      <% @mainboard.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
